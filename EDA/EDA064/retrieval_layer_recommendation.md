# Retrieval layer recommendation

Use a document-level inventory for Top-K candidate selection and a richer document-internal index for evidence retrieval. Keep layout/style/comment structures as executor-specific side channels rather than flattening all of them into generic text chunks.